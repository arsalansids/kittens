<h1>Edit <%= @kitten.name%></h1>

<% provide(:button_text, 'Create Kitten') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <%= link_to "Delete this kitty", @kitten, method: :delete,
	                              data: { confirm: "You sure?" } %>
  </div>

</div>
